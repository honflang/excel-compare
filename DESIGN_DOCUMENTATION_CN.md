# DataDiff Ops

## 1. 项目概述

本项目是一个基于 Python 的 Excel 数据对比工具，旨在比较两个 Excel 文件中同一 Sheet 的数据行，并生成对比结果文件。它支持配置多列联合唯一标识来匹配行，支持指定对比列并高亮差异单元格。

---

## 2. 方法论

### 2.1 核心思想

- 使用“联合唯一业务标识”将两张表中的行进行对齐。
- 只比较用户指定的“比对列”，避免全表盲对比造成误差。
- 将比较结果写回主文件副本，保留原始格式并添加差异标记。

### 2.2 数据处理流程

1. 读取配置文件 `config.json`。
2. 将主文件复制为结果文件，避免直接修改源数据。
3. 按配置遍历每个 Sheet：
   - 根据 `index` 或 `name` 定位 Sheet；
   - 根据 `skip_lines` 跳过前几行；
   - 提取 `unique_columns` 生成联合键；
   - 提取 `compare_columns` 进行列值对比。
4. 比较主表和副表中每个行的唯一键：
   - 唯一键完全匹配且比对列一致：标记一致；
   - 唯一键匹配但比对列不同：标记不一致，并高亮差异单元格；
   - 副表缺失：标记“他表缺失”；
   - 当前表缺失：在结果中补充标记“当前表缺失”；
   - 主键重复或副表多重匹配：特殊提示。
5. 在结果文件中附加统计信息：总行数、一致数、不一致数、缺失数、重复数等。

### 2.3 关键技术点

- `pandas` 读取 Excel 数据为 DataFrame，简化列访问。
- `openpyxl` 操作结果文件，添加背景色与批注。
- `ColumnConfig.sub` 支持字符串截取规则，用于唯一键或对比字段的子串处理，目前对比值处理仅限于截取。
- 结果文件保留主文件原始结构，新增两列：唯一键和值对比结果。

---

## 3. 实现功能说明

### 3.1 支持的功能

- 多 Sheet 对比：通过 `sheets` 数组配置多个 Sheet。
- 联合主键匹配：支持多个列联合成唯一行标识。
- 指定对比列：只对比关心的字段。
- 差异高亮：不一致的单元格填充黄色，并添加批注显示“当前值 / 他表值”。
- 统计汇总：输出总行数、一致数、不一致数、缺失数、重复数、多重匹配数。
- 可配置输出路径：`output_path` 定义结果文件输出目录。
- 支持 `skip_lines`：跳过数据区前几行，适应复杂表头场景。

### 3.2 结果输出说明

- 结果文件名称为 `compared-{主文件名（不含扩展名）}-{timestamp}.xlsx`。
- 结果文件基于主文件复制创建，保留原始格式和列顺序。
- 在每个处理 Sheet 前插入两列：
  - `A` 列：联合唯一业务标识；
  - `B` 列：对比结果描述。
- 统计区域写入结果表底部，使用颜色区分一致与异常数值。

---

## 4. 配置说明

示例配置文件：

```json
{
  "main_compare_file_path": "/path/to/main.xlsx",
  "sub_compare_file_path": "/path/to/sub.xlsx",
  "output_path": "/path/to/output",
  "skip_both": true,
  "sheets": [
    {
      "index": 1,
      "name": "Sheet1",
      "skip_lines": 0,
      "unique_columns": [
        {"name": "部门", "sub": [null, -2]},
        "系统"
      ],
      "compare_columns": [
        {"name": "状态1", "sub": [2]},
        {"name": "状态2", "sub": [1]}
      ]
    }
  ]
}
```

### 4.1 主要字段

- `main_compare_file_path`: 主文件路径。
- `sub_compare_file_path`: 副文件路径。
- `output_path`: 结果文件输出目录，未配置则在当前工作目录生成。
- `skip_both`: 当 skip 行处理中，如果当前表跳过是否同时跳过副表。
- `sheets`: Sheet 配置列表。

### 4.2 Sheet 配置

- `index`: Sheet index 从 0 开始；若同时存在 `name`，以 `index` 为准。
- `name`: Sheet 名称，当 `index` 不指定时使用。
- `skip_lines`: 从数据区开始跳过的行数，默认 0。
- `unique_columns`: 用于生成唯一键的列列表。可以使用字符串或对象两种形式表示列配置。
- `compare_columns`: 需要比较的列列表。也可使用字符串或对象两种形式。

### 4.3 列配置

- `name`: 列名。
- `sub`: 可选字符串截取规则：
  - `[n]`：取前 n 位；
  - `[start, end]`：取区间；
  - `[null, -n]`：取后 n 位。

---

## 5. 局限与风险

### 5.1 当前局限

- 仅支持 Excel 文件，不支持 CSV 或其他表格格式。
- 对比时以字符串方式比较，虽然可以比较数字、空值和大小写差异，但不一定能处理日期/数值的语义等价情况。
- 唯一键匹配通过 Python 列表查找实现，性能适合中小数据集，对于大表可能较慢。
- 如果主表或副表存在重复唯一键，结果会被标记为“重复”或“多重匹配”，但不会自动拆解。
- 对比时只支持行级单列差异高亮，不支持更复杂的逻辑条件或单元格级别权重比较。
- 结果文件基于主文件复制创建，因此副文件新增行不会保留原始格式，仅写入对比结果区域。

### 5.2 可能改进点

- 使用哈希表或 DataFrame 索引优化唯一键匹配性能。
- 增加对 CSV、ODS 等更多文件格式的支持。
- 支持更多类型的字段预处理与比较规则，例如数值标准化、货币符号清洗、日期格式统一本地化、文本空格/大小写规范化。
- 支持按列类型进行语义比较（例如日期与数值的等价判断），而不仅仅依赖字符串比较。
- 增加类似拼写检查或近似匹配的自动纠错能力，以改善字段名称或文本内容的小幅差异识别。
- 提供更灵活的差异输出格式，例如生成差异摘要表或导出 JSON/CSV。
- 增强异常处理，读取错误时给出更详细的定位信息。

---

## 6. 使用说明

1. 安装依赖：
```bash
pip install pandas openpyxl
```
2. 编辑 `config.json`，填写主文件、对比文件及 Sheet 配置。
3. 运行脚本：
```bash
python main.py
```
或指定配置文件：
```bash
python main.py my-config.json
```
4. 在 `output_path` 指定目录中查看生成的 `compared-...xlsx` 文件。
